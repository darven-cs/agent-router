---
phase: quick
plan: l16-tui-bug-zhipu
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/upstream/upstream.go
  - internal/proxy/proxy.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "用户在 TUI 中选择上游后，下次请求使用选中的上游"
    - "上游请求失败后，下一次请求自动切换到其他上游"
  artifacts:
    - path: "internal/upstream/upstream.go"
      provides: "TrackPrimaryFailure 方法，自动追踪失败的主上游"
    - path: "internal/proxy/proxy.go"
      provides: "proxyWithRetry 调用 TrackPrimaryFailure 并在下次请求时自动跳过失败主上游"
  key_links:
    - from: "internal/proxy/proxy.go"
      to: "internal/upstream/upstream.go"
      via: "lb.TrackPrimaryFailure() / lb.ShouldSkipPrimary()"
      pattern: "TrackPrimaryFailure|ShouldSkipPrimary"
---

<objective>
修复两个 TUI 和上游切换相关的 bug：
1. TUI 选择上游后仍然使用旧上游
2. Zhipu 失败后下次请求未自动切换模型
</objective>

<context>
@/home/darven/桌面/dev_app/agent-router/internal/upstream/upstream.go
@/home/darven/桌面/dev_app/agent-router/internal/proxy/proxy.go
@/home/darven/桌面/dev_app/agent-router/cmd/agent-router/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 LoadBalancer 中添加失败追踪机制</name>
  <files>internal/upstream/upstream.go</files>
  <action>
在 LoadBalancer 结构体中添加失败追踪字段：

```go
type LoadBalancer struct {
    upstreams []*Upstream
    primary   *Upstream
    mu        sync.RWMutex
    // 新增：追踪主上游失败
    primaryFailTime  time.Time     // 上次主上游失败时间
    primaryFailCount int           // 连续失败次数
}
```

添加两个新方法：

```go
// TrackPrimaryFailure 记录主上游失败，当主上游连续失败3次后自动清除
func (lb *LoadBalancer) TrackPrimaryFailure() {
    lb.mu.Lock()
    defer lb.mu.Unlock()
    lb.primaryFailCount++
    lb.primaryFailTime = time.Now()
    // 连续3次失败后清除主上游，自动切换到其他上游
    if lb.primaryFailCount >= 3 {
        lb.primary = nil
    }
}

// ShouldSkipPrimary 检查是否应该跳过主上游（基于失败状态）
func (lb *LoadBalancer) ShouldSkipPrimary() bool {
    lb.mu.RLock()
    defer lb.mu.RUnlock()
    if lb.primary == nil {
        return false
    }
    // 如果主上游连续失败3次，清除它
    if lb.primaryFailCount >= 3 {
        return true
    }
    return false
}

// ClearPrimaryFailure 清除失败计数（当主上游成功时调用）
func (lb *LoadBalancer) ClearPrimaryFailure() {
    lb.mu.Lock()
    defer lb.mu.Unlock()
    lb.primaryFailCount = 0
}
```

修改 SetPrimary 方法，在设置新主上游时清除失败计数：

```go
func (lb *LoadBalancer) SetPrimary(u *Upstream) {
    lb.mu.Lock()
    defer lb.mu.Unlock()
    lb.primary = u
    lb.primaryFailCount = 0 // 清除失败计数
}
```
  </action>
  <verify>go build ./...</verify>
  <done>LoadBalancer 添加 TrackPrimaryFailure/ShouldSkipPrimary/ClearPrimaryFailure 方法</done>
</task>

<task type="auto">
  <name>Task 2: 在 proxyWithRetry 中调用失败追踪</name>
  <files>internal/proxy/proxy.go</files>
  <action>
在 proxyWithRetry 中：

1. 请求成功时调用 `lb.ClearPrimaryFailure()` 清除失败计数
2. 请求失败且是主上游时调用 `lb.TrackPrimaryFailure()` 记录失败
3. 在 GetPrimary 检查后，额外检查 ShouldSkipPrimary

修改 proxyWithRetry 函数：

```go
func (h *ProxyHandler) proxyWithRetry(w http.ResponseWriter, r *http.Request, requestID string) {
    enabled := h.lb.GetEnabled()
    if len(enabled) == 0 {
        h.writeError(w, http.StatusBadGateway, "upstream_error", "No upstream available", 1001)
        return
    }

    var lastUpstream *upstream.Upstream
    var lastErr error
    retryCount := 0
    delay := baseDelay
    isPrimaryAttempt := false // 追踪是否在尝试主上游

    // Primary upstream check: if set, try it first
    primary := h.lb.GetPrimary()

    // Bug fix: 检查是否应该跳过主上游（连续失败后）
    if primary != nil && h.lb.ShouldSkipPrimary() {
        primary = nil
    }

    if primary != nil {
        // Verify primary is still in enabled list
        for _, u := range enabled {
            if u == primary && u.Enabled {
                lastUpstream = primary
                isPrimaryAttempt = true
                break
            }
        }
    }
    // If no primary or primary not valid, use normal selection
    if lastUpstream == nil {
        lastUpstream = h.lb.SelectNext(nil)
        isPrimaryAttempt = false
    }

    for attempt := 0; attempt <= maxRetries; attempt++ {
        upstream := lastUpstream
        if attempt > 0 {
            upstream = h.lb.SelectNext(lastUpstream)
            isPrimaryAttempt = false // 重试时不再是主上游尝试
        }
        if upstream == nil {
            upstream = enabled[0]
        }

        retryable, statusCode := h.proxyRequest(w, r, upstream, requestID, attempt, retryCount)
        if retryable == nil {
            // 成功：清除主上游的失败计数
            if isPrimaryAttempt {
                h.lb.ClearPrimaryFailure()
            }
            return // success
        }
        lastErr = retryable
        lastUpstream = upstream

        // 如果是主上游失败，追踪它
        if isPrimaryAttempt && primary != nil && upstream == primary {
            h.lb.TrackPrimaryFailure()
        }

        // Check if retryable per D-01: only timeout, 5xx, or 429
        if !isRetryable(lastErr, statusCode) {
            break
        }

        if attempt < maxRetries {
            time.Sleep(delay)
            delay *= 2
            if delay > maxDelay {
                delay = maxDelay
            }
            retryCount++
        }
    }

    // All retries exhausted - return 1001 error
    h.writeError(w, http.StatusBadGateway, "upstream_error", "All upstreams failed", 1001)
}
```
  </action>
  <verify>go build ./...</verify>
  <done>proxyWithRetry 正确追踪主上游失败并在下次请求时自动切换</done>
</task>

<task type="auto">
  <name>Task 3: 验证修复</name>
  <files>internal/upstream/upstream.go, internal/proxy/proxy.go</files>
  <action>
编译验证：

```bash
cd /home/darven/桌面/dev_app/agent-router && go build ./...
```

检查代码逻辑是否符合预期：
1. LoadBalancer 添加了 TrackPrimaryFailure/ShouldSkipPrimary/ClearPrimaryFailure
2. proxyWithRetry 在主上游成功时清除失败计数
3. proxyWithRetry 在主上游失败时增加失败计数，连续3次后自动清除主上游
4. 下次请求时，如果主上游被清除，会自动使用 SelectNext 选择其他上游
  </action>
  <verify>go build ./...</verify>
  <done>代码编译通过，修复逻辑正确</done>
</task>

</tasks>

<verification>
1. go build ./... 编译成功
2. 代码逻辑验证：
   - 主上游失败3次后自动清除
   - 清除主上游后下次请求使用 SelectNext 选其他上游
   - 手动切换主上游时清除失败计数
</verification>

<success_criteria>
- 编译通过
- 修复逻辑：
  - Bug 1: SetPrimary 正确设置主上游
  - Bug 2: 主上游连续失败3次后，下次请求自动切换到其他上游
</success_criteria>

<output>
创建完成报告到 .planning/quick/260405-l16-tui-bug-zhipu/260405-l16-SUMMARY.md
</output>
