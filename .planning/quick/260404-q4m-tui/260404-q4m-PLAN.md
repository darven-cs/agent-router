---
phase: quick
plan: 260404-q4m
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/darven/桌面/dev_app/agent-router/tui.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "User selects a model in model-select mode, the model's name is saved to that upstream's Model field"
    - "Model selection persists to config.yaml after selection"
  artifacts:
    - path: tui.go
      provides: handleModelSelect fixes
  key_links:
    - from: tui.go handleModelSelect
      to: upstream Model field
      via: us.Model assignment
---

<objective>
Fix TUI model selection so that when a user selects a model for an upstream, the model name is actually saved to that upstream and persisted to config.yaml.

Purpose: Currently handleModelSelect reads us.Model but never writes back, causing model selections to be lost on SIGHUP reload.
Output: Fixed handleModelSelect in tui.go
</objective>

<context>
@/home/darven/桌面/dev_app/agent-router/tui.go (lines 287-325 handleModelSelect)
@/home/darven/桌面/dev_app/agent-router/main.go (lines 113-126 OnUpstreamToggled and OnDefaultModelChanged callbacks)

**Bug location:** tui.go handleModelSelect (lines 287-325)

**Current buggy behavior:**
- enter/0-9 keys read us.Model and set m.defaultModel
- Does NOT update us.Model
- Model selection is lost after SIGHUP reload

**Required behavior per user decisions (260404-q4m-CONTEXT.md):**
- Each upstream independently records its own default model
- Model selection persists to config.yaml via persistConfig()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix handleModelSelect to save model to upstream</name>
  <files>/home/darven/桌面/dev_app/agent-router/tui.go</files>
  <action>
In tui.go handleModelSelect function (around line 299-307), fix the "enter" case to:

1. When user presses enter in model-select mode:
   - Get the selected upstream: us := m.upstreams[m.selectedIndex]
   - Save the current defaultModel TO the upstream: us.Model = m.defaultModel
   - Then notify via OnDefaultModelChanged (for global default)

2. Also fix the 0-9 number keys case (lines 311-321) to do the same:
   - Save m.defaultModel to us.Model before calling OnDefaultModelChanged

The key fix is: us.Model = m.defaultModel BEFORE calling OnDefaultModelChanged.

Also update OnDefaultModelChanged callback in main.go to also persist the per-upstream model. Currently OnDefaultModelChanged only updates cfg.Service.Model (global). We need to ALSO update the upstream's Model in sharedUpstreams and persist.

Actually, looking at the callbacks more carefully:
- OnUpstreamToggled already persists config via persistConfig()
- But OnDefaultModelChanged only updates global default, not per-upstream

For the model selection fix, we need to:
1. In handleModelSelect: set us.Model = m.defaultModel
2. Call a NEW callback OnUpstreamModelChanged that updates sharedUpstreams and persists

OR simpler approach:
1. In handleModelSelect, directly update us.Model on the in-memory upstream
2. Call OnDefaultModelChanged which already persists via persistConfig()

Wait - OnDefaultModelChanged doesn't know WHICH upstream was changed. We need to pass the upstream name or update the correct upstream.

Simplest fix: After setting us.Model = m.defaultModel in handleModelSelect, also update the sharedUpstreams directly and persist. But TUI model doesn't have access to sharedUpstreams directly.

Better approach: Add a new callback OnUpstreamModelSelected(upstream *Upstream) that:
1. Updates the upstream's Model in sharedUpstreams
2. Calls persistConfig()

Let me be more specific:
- Add to model struct: OnUpstreamModelSelected func(*Upstream)
- In handleModelSelect enter case, after us.Model = m.defaultModel, call OnUpstreamModelSelected(us)
- In main.go, implement: func(u *Upstream) { sharedUpstreams.Update(u.Name, u); persistConfig() }
  </action>
  <verify>go build -o /tmp/agent-router-test . && echo "Build successful"</verify>
  <done>User presses m -> model-select mode -> selects upstream model with enter -> model is saved to upstream and persists to config.yaml</done>
</task>

</tasks>

<verification>
1. Press 'm' to enter model-select mode
2. Use up/down to select an upstream
3. Press '1'-'9' to directly select a model by number
4. Press enter to confirm
5. Verify config.yaml has the correct Model for that upstream
6. Restart app and verify model selection is restored
</verification>

<success_criteria>
- Model selection now SAVES to upstream.Model field (not just reads)
- Model persists to config.yaml after selection
- After SIGHUP reload, model selection is preserved
</success_criteria>

<output>
After completion, create `.planning/quick/260404-q4m-tui/260404-q4m-SUMMARY.md`
</output>
