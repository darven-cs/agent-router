---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - config.yaml
  - Makefile
  - main.go
  - config.go
  - proxy.go
  - tui.go
  - upstream.go
  - README.md
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - UPST-01
  - UPST-02
  - UPST-03
  - UPST-04
  - LB-01
  - LB-02
  - LB-03
  - TUI-01
  - TUI-02
  - TUI-03
  - TUI-04
must_haves:
  truths:
    - "Claude SDK requests to POST /v1/messages are proxied to configured upstream and return valid Claude responses"
    - "Requests without valid x-api-key or Bearer token are rejected with 401"
    - "Service starts, binds to configured port, and displays uptime in TUI"
    - "TUI shows service name, version, port, and list of configured upstreams with enabled/disabled status"
    - "Request log in TUI shows each request with latency and upstream response status"
    - "Load balancer distributes requests evenly across enabled upstreams using modulo hash"
  artifacts:
    - path: "go.mod"
      provides: "Go module with all dependencies"
      min_lines: 15
    - path: "config.yaml"
      provides: "Service configuration with 3 upstream providers"
      contains: "upstreams:"
    - path: "main.go"
      provides: "Application entry point, starts TUI and HTTP server"
      contains: "tea.NewGoroutine"
    - path: "config.go"
      provides: "Config struct and YAML parsing with env expansion"
      exports: "Config, LoadConfig"
    - path: "proxy.go"
      provides: "HTTP handler for /v1/messages with auth validation"
      exports: "handleMessages"
    - path: "upstream.go"
      provides: "Upstream management and modulo-hash load balancing"
      exports: "Upstream, LoadBalancer, Select()"
    - path: "tui.go"
      provides: "Bubbletea TUI displaying service status and request log"
      exports: "model, Update, View"
  key_links:
    - from: "main.go"
      to: "config.go"
      via: "LoadConfig() returns Config struct"
    - from: "main.go"
      to: "tui.go"
      via: "tea.NewGoroutine starts TUI, HTTP runs in background"
    - from: "proxy.go"
      to: "upstream.go"
      via: "LoadBalancer.Select() picks upstream"
    - from: "tui.go"
      to: "proxy.go"
      via: "shared channel for request log updates"
---

<objective>
Initialize the Agent Router project with a working API proxy that routes Claude SDK requests to configured upstream providers and displays status in a basic TUI.
</objective>

<execution_context>
@/home/darven/桌面/dev_app/agent-router/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/darven/桌面/dev_app/agent-router/.planning/phases/01-foundation/01-CONTEXT.md
@/home/darven/桌面/dev_app/agent-router/.planning/ROADMAP.md
@/home/darven/桌面/dev_app/agent-router/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go project with dependencies and configuration</name>
  <files>go.mod, config.yaml, Makefile</files>
  <action>
    Create the following files with exact content:

    **go.mod** (module path: agent-router):
    ```
    module agent-router

    go 1.21

    require (
        github.com/charmbracelet/bubbletea v1.0.0
        github.com/charmbracelet/lipgloss v2.0.0
        gopkg.in/yaml.v3 v3.0.1
    )
    ```

    **config.yaml** (with 3 pre-configured upstreams):
    ```yaml
    service:
      name: "Agent Router"
      version: "1.0.0"
      port: 8080
      api_key: "${AGENT_ROUTER_API_KEY}"  # Env var expansion

    upstreams:
      - name: "Zhipu"
        url: "https://open.bigmodel.cn/api/paas/v4/messages"
        api_key: "${ZHIPU_API_KEY}"
        auth_type: "bearer"  # bearer or x-api-key
        enabled: true
        timeout: 30

      - name: "Aicodee"
        url: "https://api.aicodee.com/v1/messages"
        api_key: "${AICODEE_API_KEY}"
        auth_type: "x-api-key"
        enabled: true
        timeout: 30

      - name: "Minimax"
        url: "https://api.minimax.chat/v1/messages"
        api_key: "${MINIMAX_API_KEY}"
        auth_type: "bearer"
        enabled: true
        timeout: 30
    ```

    **Makefile**:
    ```makefile
    .PHONY: build run clean test lint

    BIN=agent-router
    GO=go

    build:
        $(GO) build -o $(BIN) .

    run: build
        ./$(BIN)

    clean:
        rm -f $(BIN)

    test:
        $(GO) test -v ./...

    lint:
        golangci-lint run

    deps:
        $(GO) mod tidy
        $(GO) mod download
    ```
  </action>
  <read_first>
    /home/darven/桌面/dev_app/agent-router/CLAUDE.md (tech stack versions per D-01/D-02)
  </read_first>
  <acceptance_criteria>
    - go.mod contains module "agent-router" with go 1.21 and 3 require directives
    - config.yaml contains service.name, service.version, service.port, service.api_key
    - config.yaml contains upstreams array with 3 entries (Zhipu, Aicodee, Minimax)
    - Each upstream has name, url, api_key (with env var), auth_type, enabled, timeout
    - Makefile contains build, run, clean, test, lint, deps targets
    - `go mod tidy` completes without error
  </acceptance_criteria>
  <verify>
    <automated>cd /home/darven/桌面/dev_app/agent-router && go mod tidy && go build -o agent-router .</automated>
  </verify>
  <done>Go module initialized with dependencies, config.yaml created with 3 upstreams, Makefile provides build/run/test commands</done>
</task>

<task type="auto">
  <name>Task 2: Implement config.go, upstream.go, proxy.go</name>
  <files>config.go, upstream.go, proxy.go</files>
  <action>
    Create **config.go**:
    - Define `ServiceConfig` struct: Name, Version, Port int, APIKey string
    - Define `UpstreamConfig` struct: Name, URL, APIKey, AuthType string (values: "bearer" or "x-api-key"), Enabled bool, Timeout int (seconds)
    - Define `Config` struct: Service ServiceConfig, Upstreams []UpstreamConfig
    - Implement `LoadConfig(path string) (*Config, error)`:
      - Read file via os.ReadFile
      - Apply `os.ExpandEnv()` to raw YAML before parsing
      - Return parsed Config
    - Use `gopkg.in/yaml.v3` for parsing

    Create **upstream.go**:
    - Define `Upstream` struct: Name, URL, APIKey, AuthType string, Enabled bool, Timeout time.Duration
    - Define `LoadBalancer` struct: upstreams []Upstream
    - Implement `NewLoadBalancer(configs []UpstreamConfig) LoadBalancer`:
      - Convert config to Upstream (timeout int->duration)
      - Filter to only enabled upstreams
    - Implement `Select(hashInput string) *Upstream`:
      - Use FNV-1a hash: `fnv.New32a([]byte(hashInput))`
      - Modulo: `hash % len(lb.upstreams)`
      - Return pointer to selected upstream
    - Implement `GetEnabled() []*Upstream` returning slice of enabled upstreams

    Create **proxy.go**:
    - Define `ProxyHandler` struct: lb LoadBalancer, apiKey string, logChan chan<- RequestLog
    - Define `RequestLog` struct: Timestamp time.Time, LatencyMs int64, UpstreamName string, StatusCode int
    - Implement `NewProxyHandler(lb LoadBalancer, apiKey string, logChan chan RequestLog) *ProxyHandler`
    - Implement `handleMessages(w http.ResponseWriter, r *http.Request)` on ProxyHandler:
      - **Auth check (CORE-02)**: Extract token from `x-api-key` header OR `Authorization: Bearer ` prefix
      - If token != apiKey, respond with 401 and body: `{"error": {"type": "authentication_error", "message": "Invalid API key"}}`
      - **Request ID extraction (LB-02)**: Get `x-request-id` header, fallback to `r.RemoteAddr`
      - **Upstream selection (LB-01)**: Call `lb.Select(requestID)`
      - **Proxy request (CORE-03)**: Create new request to upstream.URL
        - Copy headers: Host, Content-Type, x-request-id, Authorization (transform if needed)
        - For bearer auth: set `Authorization: Bearer {api_key}`
        - For x-api-key auth: set `x-api-key: {api_key}`
        - Set timeout from upstream.Timeout
        - Copy request body
      - **Send request and capture latency**
      - **Copy response** (CORE-04): Status code, headers, body back to client
      - **Log request**: Send RequestLog to logChan
      - **Error handling**: On upstream error, return 502 with `{"error": {"type": "upstream_error", "message": "...", "code": 1001}}`
  </action>
  <read_first>
    /home/darven/桌面/dev_app/agent-router/.planning/phases/01-foundation/01-CONTEXT.md (D-05 through D-25 for exact implementation specs)
  </read_first>
  <acceptance_criteria>
    - config.go defines ServiceConfig, UpstreamConfig, Config structs with exact field names
    - config.go exports LoadConfig function that applies os.ExpandEnv before yaml.Unmarshal
    - upstream.go exports Upstream, LoadBalancer structs and NewLoadBalancer, Select, GetEnabled functions
    - upstream.go Select() uses fnv.New32a() for hash, returns upstream via modulo
    - proxy.go exports ProxyHandler, RequestLog structs and NewProxyHandler, HandleMessages functions
    - proxy.go auth check: x-api-key header OR Authorization: Bearer token must equal configured api_key
    - proxy.go error responses use exact JSON format per D-23 and D-24
    - `go build ./...` completes without error
  </acceptance_criteria>
  <verify>
    <automated>cd /home/darven/桌面/dev_app/agent-router && go build -o /dev/null ./...</automated>
  </verify>
  <done>Core proxy logic implemented: config loading with env expansion, modulo-hash load balancing, authenticated HTTP proxy to upstream</done>
</task>

<task type="auto">
  <name>Task 3: Implement tui.go and main.go</name>
  <files>tui.go, main.go</files>
  <action>
    Create **tui.go** using charmbracelet/bubbletea v1 and lipgloss v2:

    Define `model` struct with fields:
    - serviceName string
    - version string
    - port int
    - startTime time.Time
    - upstreams []*Upstream (reference to load balancer's upstreams)
    - logs []RequestLog (circular buffer, max 50 entries)
    - requestCount int64
    - successCount int64

    Implement `tea.Model` interface:
    - `Update(msg tea.Msg) (tea.Model, tea.Cmd)`:
      - Handle `tea.KeyMsg`: on 'q' or ctrl+c return tea.Quit
      - Return model unchanged for other messages
    - `View() string`:
      - Use lipgloss for styling
      - **Header (TUI-01)**: serviceName, version, port, uptime counter (time.Since(startTime).String())
      - **Upstream list (TUI-02)**: Two columns - name, status (enabled=green, disabled=red), show all upstreams from config
      - **Request log (TUI-03)**: Show last 50 logs - each entry: timestamp, latency (ms), upstream name, status code
      - **Stats (TUI-04)**: Total requests, success rate percentage

    Style constants:
    ```go
    stylePrimary = lipgloss.NewStyle().Foreground(lipgloss.Color("99"))
    styleSecondary = lipgloss.NewStyle().Foreground(lipgloss.Color("245"))
    styleSuccess = lipgloss.NewStyle().Foreground(lipgloss.Color("82"))
    styleError = lipgloss.NewStyle().Foreground(lipgloss.Color("196"))
    ```

    Create **main.go**:
    - Use `tea.NewGoroutine` pattern - TUI is main, HTTP runs in background goroutine
    - On startup:
      - Load config from "config.yaml" via LoadConfig (use os.Executable() to find config)
      - Create LoadBalancer from config
      - Create logChan := make(chan RequestLog, 100)
      - Create ProxyHandler
      - Start HTTP server in goroutine: `go http.ListenAndServe(":"+port, handler)`
      - Start TUI: `p := tea.NewProgram(model, tea.WithAltScreen())`
      - Run: `p.Run()`
    - Graceful shutdown: on TUI quit, HTTP server shuts down gracefully
  </action>
  <read_first>
    /home/darven/桌面/dev_app/agent-router/.planning/phases/01-foundation/01-CONTEXT.md (D-14 through D-18 for TUI architecture)
    /home/darven/桌面/dev_app/agent-router/CLAUDE.md (bubbletea v1, lipgloss v2 per tech stack)
  </read_first>
  <acceptance_criteria>
    - tui.go defines model struct with all required fields (serviceName, version, port, startTime, upstreams, logs, requestCount, successCount)
    - tui.go implements tea.Model interface with Update and View methods
    - tui.go View() renders: header with service info, upstream list with status colors, request log (max 50 entries), stats
    - tui.go handles q/ctrl+c for graceful shutdown
    - main.go uses tea.NewGoroutine pattern
    - main.go creates LoadBalancer, ProxyHandler, logChan, starts HTTP in goroutine
    - main.go passes port from config to HTTP server
    - `go build -o agent-router .` completes successfully
  </acceptance_criteria>
  <verify>
    <automated>cd /home/darven/桌面/dev_app/agent-router && go build -o agent-router . && test -f agent-router && rm agent-router</automated>
  </verify>
  <done>Working TUI displaying service status, upstream list, and request logs; HTTP server running in background goroutine</done>
</task>

<task type="auto">
  <name>Task 4: Create README.md</name>
  <files>README.md</files>
  <action>
    Create README.md with the following sections:

    ```markdown
    # Agent Router

    Local API proxy that routes Claude Code requests to multiple upstream providers (Zhipu, Aicodee, Minimax) with automatic failover and load balancing.

    ## Features

    - **Claude API Compatible**: Exposes POST /v1/messages endpoint fully compatible with Claude official SDK
    - **Multi-Upstream Support**: Configure multiple upstream providers with enable/disable toggle
    - **Load Balancing**: Modulo-hash distribution across enabled upstreams
    - **Real-time TUI**: Monitor service status, upstream health, and request logs
    - **Authentication**: API key validation on every request

    ## Quick Start

    ### Prerequisites

    - Go 1.21+
    - API keys for at least one upstream provider

    ### Configuration

    1. Copy `config.yaml` and set your API keys:
       ```bash
       export AGENT_ROUTER_API_KEY="your-router-api-key"
       export ZHIPU_API_KEY="your-zhipu-key"
       export AICODEE_API_KEY="your-aicodee-key"
       export MINIMAX_API_KEY="your-minimax-key"
       ```

    2. Edit `config.yaml` to enable/disable upstreams and adjust timeouts

    ### Build

    ```bash
    make deps
    make build
    ```

    ### Run

    ```bash
    ./agent-router
    ```

    ### Usage

    Send requests to the proxy:
    ```bash
    curl -X POST http://localhost:8080/v1/messages \
      -H "Content-Type: application/json" \
      -H "x-api-key: your-router-api-key" \
      -d '{"model": "claude-3-5-sonnet", "messages": [{"role": "user", "content": "Hello"}]}'
    ```

    ### TUI Controls

    - **q** or **Ctrl+C**: Quit

    ## Configuration

    | Field | Description |
    |-------|-------------|
    | service.port | HTTP server port |
    | service.api_key | API key for authentication |
    | upstreams[].name | Display name |
    | upstreams[].url | Upstream API endpoint |
    | upstreams[].api_key | Upstream API key (supports env vars) |
    | upstreams[].auth_type | "bearer" or "x-api-key" |
    | upstreams[].enabled | Enable/disable upstream |
    | upstreams[].timeout | Request timeout in seconds |

    ## Architecture

    - `main.go`: Entry point, TUI orchestration
    - `config.go`: Configuration loading with env expansion
    - `proxy.go`: HTTP handler with authentication and proxying
    - `upstream.go`: Load balancer with modulo-hash distribution
    - `tui.go`: Bubbletea TUI for monitoring
    ```
  </action>
  <read_first>
    /home/darven/桌面/dev_app/agent-router/config.yaml (reference for configuration documentation)
  </read_first>
  <acceptance_criteria>
    - README.md contains ## Features section listing API compatibility, multi-upstream, load balancing, TUI, authentication
    - README.md contains ## Quick Start section with build and run instructions
    - README.md contains ## Configuration section with table describing all config fields
    - README.md contains ## TUI Controls section
    - README.md contains curl example with x-api-key header
    - README.md references make commands (make deps, make build)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "##" /home/darven/桌面/dev_app/agent-router/README.md && grep -c "upstreams" /home/darven/桌面/dev_app/agent-router/README.md</automated>
  </verify>
  <done>README.md provides complete documentation for users to build, configure, and run Agent Router</done>
</task>

</tasks>

<verification>
Phase 1 is complete when:
- `go build -o agent-router .` produces working binary
- Binary starts and binds to configured port
- POST /v1/messages with valid api_key returns upstream response
- POST /v1/messages without api_key returns 401
- TUI displays service name, version, port, uptime
- TUI displays upstream list with enabled/disabled status
- Request log shows latency and status after each request
- Load balancer distributes requests using modulo hash of request ID
</verification>

<success_criteria>
1. Claude SDK requests to POST /v1/messages are proxied to configured upstream and return valid Claude responses
2. Requests without valid x-api-key or Bearer token are rejected with 401
3. Service starts, binds to configured port, and displays uptime in TUI
4. TUI shows service name, version, port, and list of configured upstreams with enabled/disabled status
5. Request log in TUI shows each request with latency and upstream response status
6. Load balancer distributes requests evenly across enabled upstreams using modulo hash
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>
